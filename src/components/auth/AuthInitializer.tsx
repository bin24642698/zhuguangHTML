'use client';

import React from 'react';

/**
 * 认证初始化组件 - 静态导出版本
 *
 * 注意：这是为了静态导出而修改的临时版本
 * 在实际部署时应该使用完整的认证初始化逻辑
 */
export default function AuthInitializer() {
  // 静态导出版本不执行任何初始化
  // 这个组件不渲染任何内容
  return null;
}
